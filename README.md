<div align="center">

# 🏆 КейсПортал · Союз изобретателей

**Платформа для кейс-чемпионатов, лекций и изобретательства**
от регистрации команд до дипломов — витрина изобретений, инвесторы и наставники
с ИИ-ассистентом организатора

[![Next.js](https://img.shields.io/badge/Next.js%2016-black?logo=next.js)](https://nextjs.org)
[![React](https://img.shields.io/badge/React%2019-61DAFB?logo=react&logoColor=black)](https://react.dev)
[![Supabase](https://img.shields.io/badge/Supabase-3FCF8E?logo=supabase&logoColor=black)](https://supabase.com)
[![TypeScript](https://img.shields.io/badge/TypeScript-3178C6?logo=typescript&logoColor=white)](https://www.typescriptlang.org)
[![MCP](https://img.shields.io/badge/MCP-server-7c3aed)](mcp/)
[![License: MIT](https://img.shields.io/badge/License-MIT-gold.svg)](LICENSE)

[Возможности](#возможности) · [Союз изобретателей](#союз-изобретателей) · [Быстрый старт](#быстрый-старт) · [MCP](#mcp-сервер--ии-ассистент-организатора) · [REST API](#rest-api-v1--webhooks) · [Архитектура](#структура-проекта)

</div>

---

## Что это

Организатор публикует кейс-чемпионат или лекцию → команды регистрируются и выбирают кейсы → загружают решения → жюри оценивает по критериям (вслепую!) → таблица лидеров с честной нормализацией оценок → дипломы участникам.

Для **Союза изобретателей** портал — ещё и дом для разработок: витрина изобретений с контролем раскрытия, каталог инвесторов с умным матчингом и программа наставничества. При этом организатор может вообще **не открывать браузер** — весь цикл управляется через ИИ-ассистента (Claude, Cursor) по протоколу **MCP**.

### Ключевые фишки

| | |
|---|---|
| 🧠 **Умная лента** | персональные скоринги: теги интересов + нейро-эмбеддинги BGE-M3 (pgvector), затухание сигналов по времени, «с этим событием также» |
| 🕶️ **Слепое судейство** | судьи не видят авторство работ до публикации итогов — оценка без предвзятости |
| ⚖️ **Нормализация оценок** | z-score калибровка компенсирует «строгих» и «щедрых» судей, индекс 0–100 |
| 💡 **Витрина изобретений** | стадии от идеи до рынка, патентный статус, **трёхуровневая видимость** — публичное раскрытие до патентной заявки не уничтожает новизну |
| 🤝 **Мэтчинг с инвесторами** | секторы + косинусная близость эмбеддингов: портал сам подбирает инвестора под изобретение |
| 🧭 **Наставничество** | профили менторов, заявки, активные связи с лимитом мест и журналом сессий |
| 🏢 **Мультиарендность** | организации с ролями owner / admin / manager, командная работа над событиями |
| 🤖 **MCP-сервер** | 15 инструментов для ИИ-ассистента: события, заявки, судьи, итоги, изобретения, наставники |
| 🔌 **REST API + Webhooks** | интеграция с HR-системами, HMAC-подпись событий |
| 🔒 **RLS везде** | Row Level Security на всех таблицах, ключи хранятся как sha256 |

## Возможности

<details>
<summary><b>Для участников</b> (раскрыть)</summary>

- витрина событий с персональными рекомендациями и поиском по смыслу
- регистрация команды с выбором кейса, лист ожидания при лимите мест
- загрузка решений (Supabase Storage), отслеживание статуса заявки
- таблица лидеров: обычные баллы и нормализованный индекс
- автоматические дипломы участников
- календарь событий, избранное, подписки на организаторов
- внутренние уведомления на все ключевые события

</details>

<details>
<summary><b>Для изобретателей</b> (раскрыть)</summary>

- витрина разработок: стадия (идея → прототип → MVP → патент → рынок), патентный статус, цель инвестиций
- **контроль раскрытия**: `публично / только инвесторам / только мне` — работа видна одобренным инвесторам, но не индексируется и не лишает патентной новизны
- питч-дек в приватном Storage-бакете: доступ только у автора и инвесторов с заявкой
- рекомендованные инвесторы под каждое изобретение (секторы + семантика описания)
- заявки инвесторов на контакт с уведомлениями, статусы «на рассмотрении / принято / отклонено»
- связь изобретения с событием-источником: работа продолжает решение с чемпионата

</details>

<details>
<summary><b>Для наставников и менти</b> (раскрыть)</summary>

- профиль наставника: экспертиза, стаж, формат (онлайн/очно), вместимость — сколько менти вести одновременно
- заявки на наставничество с контекстом конкретного изобретения
- активные связи: лимит `capacity` проверяется в БД, приём сверх лимита невозможен
- журнал сессий: итоги обсуждений и следующие шаги, видны только паре
- уведомления о заявках и ответах наставника

</details>

<details>
<summary><b>Для организаторов</b> (раскрыть)</summary>

- создание мероприятий: кейсы, критерии оценки, темы, обложки, лимит мест
- слепое судейство одним переключателем
- приглашение судей по e-mail, подтверждение с их стороны
- дашборд: воронка заявок, статистика решений, состав жюри, экспорт XLSX
- публикация итогов → решения и оценки становятся публичными
- организации: несколько организаторов ведут общие события

</details>

<details>
<summary><b>Интеграции</b> (раскрыть)</summary>

- **MCP** ([mcp/](mcp/)) — управление чемпионатами через Claude Desktop / Cursor
- **REST API v1** ([docs/API.md](docs/API.md)) — события, заявки, лидерборды
- **Webhooks** — `registration.created`, `solution.uploaded`, `results.published` с HMAC-подписью
- **API-ключи** — sha256-хэш, мгновенный отзыв

</details>

## Союз изобретателей

Три контура модуля закрывают путь изобретателя от идеи до контракта:

1. **Витрина `/inventions`** — разработки с фильтром по стадиям и поиском; карточка показывает стадию, секторы, цель инвестиций и замок видимости. Один флаг `visibility = investors_only` решает главный страх: потенциальные инвесторы видят работу, но публика — нет, значит патентная новизна сохранена.
2. **Инвесторы `/investors`** — каталог фондов, бизнес-ангелов и корпораций с тезисами и чеками, с модерацией профилей Союзом. Матчинг `recommended_investors()` считает пересечение секторов ×2 и косинусную близость эмбеддингов BGE-M3 ×4 — автору сразу видно, кому писать.
3. **Наставничество `/mentorship`** — программа менторов: заявки с контекстом изобретения, активные связи с лимитом мест (проверка в триггере БД) и журнал сессий «итоги + следующие шаги».

Сценарий для демо (аккаунты ниже): Егор публикует «Гелиотрон» → фонд «Генезис» видит его в подборе, подаёт заявку на контакт → автор принимает → наставник ведёт Егора к международной заявке PCT, всё фиксируется в журнале.

## Быстрый старт

### 1. Supabase

Создайте проект на [supabase.com](https://supabase.com), затем:
**Dashboard → Connect → Session pooler** (порт 5432) → скопируйте URI,
замените `[YOUR-PASSWORD]` на пароль БД (**Settings → Database**).

### 2. Переменные окружения

```bash
cp .env.example .env.local
# заполните: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY,
#             DATABASE_URL (для миграций)
```

### 3. Миграции — автоматически

```bash
npm install
npm run db:migrate        # все миграции из supabase/migrations (журнал версий в БД)
npm run db:seed           # опционально: демо-данные (anna@demo.ru / demo1234)
```

### 4. Портал

```bash
npm run dev               # http://localhost:3000
```

<details>
<summary><b>Демо-аккаунты для презентации</b> (пароль у всех: <code>demo1234</code>)</summary>

| Аккаунт | Кто это | Что показать |
|---|---|---|
| `anna@demo.ru` | Анна К. — организатор | дашборд, заявки, судьи, публикация итогов |
| `egor@demo.ru` | Егор М. — изобретатель | «Мои изобретения»: «Гелиотрон» на рынке + журнал наставничества |
| `alisa@demo.ru` | Алиса В. — изобретатель | «ЭкоСкан» под патентной заявкой, видимость «только инвесторам» |
| `investor1@demo.ru` | Фонд «Генезис» — одобренный инвестор | закрытые изобретения, принятая и ожидающая заявки на контакт |
| `investor2@demo.ru` | Игорь Ангельский — инвестор на модерации | профиль не виден в каталоге, закрытые работы недоступны |
| `mentor1@demo.ru` | Виктор Патентовед — наставник | 2 из 3 мест занято, входящая заявка «на рассмотрении» |
| `mentor2@demo.ru` | Светлана И. — наставник на модерации | профиль ждёт одобрения Союза |
| `petr@demo.ru`, `maria@demo.ru` | судьи | слепое судейство, оценки |

</details>

<details>
<summary><b>Опционально: умная лента и API</b></summary>

```bash
# нейро-эмбеддинги ленты (локально):
ollama serve && ollama pull bge-m3
npm run embed:backfill

# REST API /api/v1 и webhooks — добавьте в .env.local:
SUPABASE_SECRET_KEY=sb_secret_...
```

</details>

## MCP-сервер — ИИ-ассистент организатора

```bash
cd mcp && npm install && npm run build
```

Подключение к Claude Desktop (`~/Library/Application Support/Claude/claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "caseportal": {
      "command": "node",
      "args": ["/путь/к/порталу/mcp/dist/index.js"],
      "env": {
        "NEXT_PUBLIC_SUPABASE_URL": "https://xxxx.supabase.co",
        "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY": "eyJ...",
        "MCP_ORG_EMAIL": "organizer@example.com",
        "MCP_ORG_PASSWORD": "..."
      }
    }
  }
}
```

15 инструментов: `create_event` (событие + кейсы + критерии одним вызовом),
заявки и статусы, `invite_judges`, `get_works`, `draft_scores` (черновик оценок),
`publish_results`, `export_report`, организации, а также модуль Союза
изобретателей: `list_inventions` (витрина с фильтрами), `match_investors`
(подбор инвесторов под изобретение), `request_mentor` (заявка на наставничество).
Подробности: [mcp/README.md](mcp/README.md).

Пример в чате: *«Создай кейс-чемпионат по аналитике с кейсами "Прогноз погоды"
и "Рекомендации", критериями Сложность 10 и Практичность 10, слепое судейство
включено, пригласи судей petr@… и maria@…»* — или про модуль изобретателя:
*«Покажи витрину изобретений на стадии прототипа из энергетики и подбери
инвесторов для первого»*.

## REST API v1 + Webhooks

```bash
curl http://localhost:3000/api/v1/events?limit=5                    # каталог (публично)
curl -H "Authorization: Bearer cp_..." /api/v1/events/:id/registrations
curl /api/v1/events/:id/leaderboard?mode=normalized                 # z-score рейтинг
```

Ключ: RPC `create_api_key` → `cp_...` (показывается один раз).
Webhooks с подписью `X-CasePortal-Signature: sha256=<HMAC>`; события —
`registration.created`, `registration.status_changed`, `solution.uploaded`,
`results.published`, `contact_request.created`.
Полная документация: **[docs/API.md](docs/API.md)**.

## Структура проекта

```
app/
  page.tsx                      витрина: hero + умная лента
  events/[id]                   событие, кейсы, регистрация
  inventions/                   витрина изобретений Союза + карточка работы
  investors/                    каталог инвесторов (модерация профилей)
  mentorship/                   наставники: каталог, заявки, связи + журнал сессий
  leaderboard                   таблица лидеров (raw / normalized)
  calendar, works, diploma/     календарь, галерея работ, дипломы
  join/[code], join/org/[token] приглашения в команду / организацию
  my/                           личный кабинет
    orgs/                       организации: создание, участники, приглашения
    events/new                  создание события (кейсы, критерии, слепое судейство)
    events/[id]                 статистика, заявки, решения, судьи
    inventions/                 мои изобретения: CRUD + видимость + питч-дек
    investor/, mentor/          профили инвестора и наставника
    registrations               мои заявки + загрузка решений
  judge/                         панель судьи (анонимизированные карточки работ)
  api/v1/                        REST API: events, registrations, leaderboard, webhooks
  api/embed                      эмбеддинги (Ollama / OpenAI-совместимый API)
lib/                             supabase, auth, api-key middleware
mcp/                             MCP-сервер для ИИ-ассистентов (15 инструментов)
supabase/
  migrations/                   нумерованные миграции (автоприменение)
  seed.sql                      демо-данные чемпионатов
  seed_inventors.sql            демо-данные модуля «Союз изобретателей»
scripts/
  db-migrate.mjs                npm run db:migrate — автоматические миграции
  db-seed.mjs                   npm run db:seed
  backfill-embeddings.mjs       бэкфилл эмбеддингов
docs/API.md                     REST API v1
```

## Безопасность

- **RLS** на всех таблицах; сервисная роль — только на сервере для проверки API-ключей
- API-ключи: sha256-хэш в БД, мгновенный отзыв, пространство: организация или пользователь
- Webhooks: HMAC-SHA256 с уникальным секретом на подписку
- MCP работает под учёткой организатора — RLS ограничивает только его данными
- Изобретения: `visibility=investors_only` видят только одобренные инвесторы и автор; питч-деки — в приватном бакете по подписанным ссылкам
- Наставничество: журнал виден только паре «наставник–менти»; лимит мест enforced триггером БД

## Команды

| Команда | Назначение |
|---|---|
| `npm run dev` | dev-сервер портала |
| `npm run build` / `npm start` | прод-сборка |
| `npm run lint` | ESLint |
| `npm run db:migrate` | применить миграции (атомарно, с журналом) |
| `npm run db:migrate -- --dry` | показать очередь без выполнения |
| `npm run db:seed` | демо-данные (чемпионаты + Союз изобретателей) |
| `npm run embed:backfill` | эмбеддинги событий |
| `cd mcp && npm run build` | сборка MCP-сервера |

## Роли и доступ (RLS)

| Роль | Права |
|---|---|
| Аноним | читает опубликованные события, кейсы, лидеров, публичные изобретения, каталог инвесторов/наставников |
| Пользователь | регистрируется, создаёт события и изобретения, видит свои заявки/решения |
| Одобренный инвестор | видит изобретения с `investors_only`, подаёт заявки на контакт |
| Одобренный наставник | принимает заявки, ведёт связи, пишет в журнал |
| Член организации | управляет событиями организации (owner/admin/manager) |
| Судья | видит решения, ставит оценки; при слепом судействе — без авторства |

## Лицензия

[MIT](LICENSE)

---

<div align="center">

**КейсПортал** — чемпионаты без рутины, изобретения без барьеров 🚀

</div>
